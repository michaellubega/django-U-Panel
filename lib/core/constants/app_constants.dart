/// App-wide constants for U-Panel.
class AppConstants {
  AppConstants._();

  static const String appName = 'U-Panel';
  static const String appTagline = 'University operations platform';

  /// Breakpoint: above this width use desktop layout (sidebar).
  static const double desktopBreakpoint = 840;

  /// Breakpoint: above this use tablet layout if needed.
  static const double tabletBreakpoint = 600;
}
